import { ChangeCharacterPipe } from './change-character.pipe';

describe('ChangeCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
